---
"@charcuterie/ui": minor
---

Add `TimecodeInput`, a typed `hh:mm:ss.mmm` media position field with an `isRange` mode for
a section of a file, plus the exported `parseTimecodeInput`, `formatTimecode` and
`clampTimecode`.
