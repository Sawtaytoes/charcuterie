---
"@charcuterie/server": minor
"@charcuterie/ui": minor
---

Detect replaced static-SPA deployments with no-cache build markers and an SSE reconnect signal. `useDeploymentUpdate` exposes a safe user-controlled reload action.
