# Decision records

One decision per file, `YYYY-MM-DD-<kebab-slug>.md`, newest first.

**Never edit a past decision to change its meaning** — supersede it with a new dated file
and cross-link both directions. Before proposing a change, read this index: a settled
decision overrides an instinct.

| Date | Decision | Summary |
| --- | --- | --- |
| 2026-08-19 | [The board owns the move, and takes no drag-and-drop dependency](2026-08-19-the-board-owns-the-move-and-takes-no-drag-and-drop-dependency.md) | **Owner: "Wow, that's barebones. Nothing like the original prototype HTML version."** `Board` ships with no DnD library: one handle per card, a `Menu` of the other lanes as the **primary** move (the only path that works by keyboard, by screen reader, and in the Narrow View where the other lanes are off screen), plus Pointer-Event dragging at **1.4 KB gzip** against 7.0/17.3/31.6 KB measured for pragmatic-dnd / dnd-kit / hello-pangea. Also draws the lines: lanes, counts, `+ n more` and empty states are the component's; data, sorting and virtualisation are the consumer's; the cross-lane "needs attention" panel is an `Alert` above the board, not a fourth lane. A lane is a `<div role="group">`, not a landmark. |
| 2026-08-19 | [A date is a calendar date, not an instant](2026-08-19-a-date-is-a-calendar-date-not-an-instant.md) | `DatePicker` carries an ISO `YYYY-MM-DD` string, never a `Date` — an instant is two different calendar dates and a due date must not shift with the browser's zone (pinned under UTC+14 and UTC-11). No date library and no `Date` arithmetic: integer day numbers plus `Intl`; `Temporal` is ~50 KB gz of polyfill away. Typed input (`tomorrow`, `next fri`, `+14d`, `8/19`) is an ordered, total grammar that **never guesses in silence** — ambiguity is a named failure and every resolution is echoed in a live region before commit. Range is `isRange`, a mode, on the `Combobox` `isMultiple` precedent. |
| 2026-08-19 | [The markdown editor is a `<textarea>` with a painted layer, not an editor core](2026-08-19-the-markdown-editor-is-a-textarea-with-a-painted-layer.md) | **`MarkdownEditor` adds no runtime dependency, and the numbers are measured rather than argued.** `esbuild --bundle --minify`, React external, `gzip -9`: TipTap 3.30 + starter-kit + `tiptap-markdown` **238 KB gz**, CodeMirror 6 `basicSetup` + `lang-markdown` **206 KB**, a hand-assembled CM6 minimum **176 KB**, Lexical 0.49 with the markdown/list/link/code nodes **148 KB** — against **4.4 KB gz of new code and zero new packages** here. `castkit/packages/slatecast` has 60 KB gz of total budget and is why `"./src/*"` is in this package's `exports`; every core is 2.5x-4x that on its own. Even a *parser* was refused (`marked` 12.6 KB, `micromark` 15.3 KB) because the painted layer wants **character ranges**, not an AST and not HTML — a parser that returns HTML discards the offsets and produces the one artefact the component exists to keep out of the data. The surface is a real `<textarea>`, which is the half that cannot be bought back later: caret movement, word-wise selection, IME composition, the platform's undo stack, `role="textbox"` and `aria-multiline` arrive free and correct in every browser, and the **no-HTML guarantee becomes structural** — a textarea takes only `text/plain` off the clipboard, every command returns markdown, the layer emits text nodes, so there is no sanitiser to get wrong. An editor core inverts that: its document model is the truth and markdown is a serialisation, which is precisely how the tracker Docket replaces lost data. Three things follow and are part of the decision: **markers are dimmed, not hidden** (concealing `**` changes the line's width and the caret lives in the textarea), so "raw syntax on the cursor's line" is a contrast change — muted everywhere, `intent-accent-content` on the caret's line; **every span style must be metric-neutral**, faux bold via `text-shadow` and a real italic only because the surface is monospaced, asserted by **measuring** a probe span against plain text in real chromium rather than by describing the rule; and **true concealment is a staged follow-up** that needs CodeMirror 6 and would ship as an optional subpath with an optional peer, the same packaging as `@charcuterie/ui/react-router`, so nobody pays for it by default. Licences MIT throughout and no candidate is Chinese-origin — checked, and recorded so it is not rechecked. What it gives up, stated so a reversal is argued on the right grounds: no concealment, no in-editor image rendering, no block-level type scaling, no structural block editing. |
| 2026-08-19 | [`categorical` is a curated palette; `Swatch` is ungoverned colour](2026-08-19-categorical-is-a-curated-palette-not-ungoverned-colour.md) | **Owner: "We might need to update Charcuterie to add more badge colors that aren't semantic. Just numerical ones… where the badges are user-chosen."** A second multi-value colour family, `--color-categorical-<n>-<role>`, numbered 1–10 and non-semantic, with the same seven roles an intent has — because every `intent` member is a *claim* (`danger` says what happens if you press the thing) and a "Homelab" label is not a `danger`. Does **not** supersede `Swatch.tsx`'s argument that a user-chosen colour is data rather than a token; it draws the boundary. `Swatch` takes colour **arriving from the world** — a physical sticker, an album accent — arbitrary, unthemeable, uncheckable, and re-theming it would be re-theming the hardware. `categorical` is a **curated set the user picks from**: finite, re-themed per variant, gated in both schemes, so the user *cannot* pick something unreadable. The misuse test is what each one discards — `categorical` for a sticker discards the data; `Swatch` for a palette pick discards the guarantee. `Badge` takes `categorical`, mutually exclusive with `intent` in the type, because a badge is one colour. |
| 2026-08-19 | [The categorical family has ten hues, and they are gated against each other](2026-08-19-the-categorical-family-has-ten-hues.md) | **Contrast to the background is not the property a categorical palette lives on — telling swatch 3 from swatch 4 is, and no contrast audit can see it.** Two indexes can both clear 4.5:1 on the same surface and be the same colour as each other with every number on the board green. So a second gate ships: every pair, every role, measured in OKLab, floors per role (`solid` 0.08, `content`/`border` 0.05, the pale tints far lower and stated as drift alarms rather than as a claim that ten tints are individually recognisable). The count is **ten** because that is the most that clears an **external** benchmark — Tableau 10's own tightest pair is **0.0835** and this ring achieves **0.0893**; twelve fails everywhere, always in the green-to-teal arc, where the sRGB gamut collapses at the lightness a legible fill needs. For scale: Radix's ten accents measure 0.0558, *below* our floor. The ring is **generated**, not typed — 560 hexes across 4 variants x 2 schemes — solved in OKLCh against the emitted hex, and `solveLightness` **throws** rather than settling, so *fix the hue, don't lower the gate* is mechanical. Hue spacing is deliberately uneven (coordinate ascent against the gamut's real shape); every fill carries a **near-black** label in both schemes, unlike an intent's white, because a fill dark enough for white is a fill pushed down the gamut; the generator targets 7:1 for tint text while the gate stays 4.5:1, because the hand-made intents measure 6.1–11.6:1 and a new family at exactly 4.5 would read as a bug. ePaper collapses all ten to black on purpose — four chromatic inks cannot carry ten indexes, and round-robin would make 1 and 6 identical. 63 gated pairs per scheme becomes **113**. |
| 2026-08-19 | [A categorical border is gated at 3:1 where an intent border is exempt](2026-08-19-categorical-borders-are-gated-where-intent-borders-are-exempt.md) | **The exemption's own justification does not survive the move.** `contrastAudit.ts` reports intent borders without gating them because "a badge outline is decorative — the meaning is carried by its text"; an intent pill says `failed` and its red is a second copy of that. A categorical pill says "Homelab", which tells you nothing about the colour, and the colour is what the eye scans a thirty-row list by — so the boundary is doing the identification WCAG 1.4.11 is about, and it is gated like one. The intent exemption is **untouched** (its measured range is 1.30–3.37:1 and most of it would fail), and `contrast.test.ts` asserts the difference in *both* directions so a later edit cannot quietly align them. Cost is a visibly heavier outline than an intent badge's, which is also what keeps a necessarily-pale tint distinguishable from its neighbour. Solved to 3.15 rather than 3.00, because a value sitting exactly on its threshold re-breaks on the next tweak — which is where `hairline`'s dark `danger.solid` sits at 4.50:1 today. |
| 2026-08-19 | [`className` is the outermost box a component renders](2026-08-19-classname-is-the-outermost-box-a-component-renders.md) | `Select` forwarded `className` to the inner `<select>` while the chevron stayed pinned to a `w-full` wrapper — **869.6px** adrift, and no way to size a `Select` at all ([#112](https://github.com/Sawtaytoes/charcuterie/issues/112)). `className` is now the outermost element in every component, no exceptions; an inner element gets its own named prop (`controlClassName`), documented as not-for-sizing. Asserted as a measurement, because every class-name assertion passed while it was broken. |
| 2026-08-16 | [`MediaTile` owns its interactive states](2026-08-16-mediatile-owns-its-interactive-states.md) | **Owner: "We should be always putting accessibility states like that in Charcuterie."** A clickable tile is `onClick` or `href` — pointer, hover fade, focus-visible ring. Do not wrap `MediaTile` in a bare `<button>`; preflight leaves a text cursor and the wrapper has none of the chrome. |
| 2026-08-16 | [`streams` may export MQTT as a Node entry](2026-08-16-streams-may-export-mqtt-as-a-node-entry.md) | **Owner: "MQTT could be part of streams as a different export. I don't remember denying it."** The 2026-07-31 "not now or later" MQTT ban was an agent's reading, not an owner call. `@charcuterie/streams/mqtt` is allowed; `streams` itself is still unbuilt (M7 design-only). Published client today is `@charcuterie/server/mqtt@0.3.0` — do not block consumers on creating `streams`. |
| 2026-08-16 | [MQTT cmd/resp lives at `@charcuterie/server/mqtt`](2026-08-16-mqtt-cmd-resp-is-charcuterie-server-mqtt.md) | **Owner: "If Charcuterie doesn't have an MQTT setup, we should add it there."** Node-only `cmd/*` / `resp/*` on the already-published `@charcuterie/server` package (a subpath, not a new `@charcuterie/mqtt` — new publishes need a manual OIDC seed). Commands/responses are never retained; overlapping commands reject with `already-running`. `mqtt` is an optional peer, same packaging as `./vite`. Revises the 2026-07-31 streams row that left MQTT as "one implementation, not yet duplication"; `streams` stays browser and push-only. `truenas-mqtt`'s `trigger`/`status` tree is a legacy special case. |
| 2026-08-16 | [A Picker is flush — the parent owns the gap](2026-08-16-picker-is-flush-the-parent-owns-the-gap.md) | **QueuePilot: "we still need a space between this listbox and the text… update either Charcuterie or the docs at least."** `Picker`/`Button`/`Select` ship no outer margin. A flush label is the parent forgetting the gutter, not a missing style on the control. Stacked → `Field`; inline → a `gap-*` row. Do not put margin on `Picker` — it would double `Field`'s `gap-1.5`. QueuePilot already had a one-off `#setmodal .fieldselect { margin-left: 10px }` for the same trap. |
| 2026-08-15 | [The `optimizeDeps.include` list is its own module, checked by CI](2026-08-15-the-optimizedeps-list-is-its-own-module-checked-by-ci.md) | **The mechanism for [the subpath rule](2026-08-15-a-new-subpath-import-must-join-the-consumers-optimizedeps-list.md), which a comment had already failed to enforce twice.** Two repos carried a detailed comment describing the hazard and drifted anyway; the audit found **every repo in the fleet that kept such a list had the same gap — 3 for 3** (mux-magic short three, rip-deck four, board-games four), and **two of mux-magic's three missing entries pre-dated the change that surfaced them**. `charcuterie-check-optimize-deps` (`@charcuterie/vitest-config@1.1.0`) diffs the declared list against the optimizer's own `_metadata.json` and fails with paste-ready names. Three details are load-bearing and each is a mistake already made: **its own `.js` module**, because the checker runs in a plain Node process that cannot load a TypeScript Vitest config; **after the suite, not during**, because the optimizer writes metadata as it discovers, so a check inside the run under-reports; and **the cache lives beside the package** (`packages/web/node_modules/.vite`), not at the repo root — clearing the wrong one gives a warm run that passes and proves nothing, an error made twice while diagnosing the original incident. A `.d.ts` accompanies the `.js` (the list stays plain JS for the checker, but `vitest.config.ts` needs it typed under `recommendedTypeChecked`), and the `readonly` array is spread into Vite's mutable `include`. Verified to **fail when it should**, not merely to pass: deleting `@charcuterie/logic/query` from rip-deck's list exits 1 and names it, while that same cold run still passed **1293/1293** — the gap between "tests green" and "list wrong" being the entire reason the check exists. |
| 2026-08-15 | [A `logic` major requires bumping `ui` in the same change](2026-08-15-a-logic-major-requires-bumping-ui-in-the-same-change.md) | **`@charcuterie/ui` pins `@charcuterie/logic` at an EXACT version, so bumping `logic` alone is not a partial upgrade — it is a broken one.** `logic@2.0.0` was the first `logic` major since `ui` existed, and mux-magic's adoption took it while leaving `ui@^2.14.0` (which pins `logic@1.5.0`): both installed, `node_modules/@charcuterie/logic` **and** `node_modules/@charcuterie/ui/node_modules/@charcuterie/logic`. Two copies of `logic` are two copies of its React bindings — the app's provider writes to one module instance, `ui`'s components read the other — surfacing as **12 tests failing with `Cannot read properties of null (reading 'useMemoCache')`**, a message naming neither package. Fix is one line: take the matching `ui` (2.15.1). Two multipliers recorded because each cost a full round: **a warm `node_modules` hides it entirely** (local runs passed 2946/2946 twice against a tree that did not match the lockfile — only `rm -rf node_modules && yarn install --immutable` reproduced it), and **the job had no `timeout-minutes`**, so the first two runs hung to GitHub's 6-hour ceiling and reported `cancelled`, which reads as an infra flake rather than a failing test. The exact pin stays — `ui` and `logic` ship as a matched pair and a range would let untested combinations float — but the coupling now has to be stated. Check: `find node_modules -name package.json -path '*@charcuterie/logic*'` must return exactly one line. |
| 2026-08-15 | [A new subpath import must join the consumer's `optimizeDeps.include`](2026-08-15-a-new-subpath-import-must-join-the-consumers-optimizedeps-list.md) | **Two repos carried a comment describing this exact hazard, and both drifted anyway — a documented manual invariant is not a maintained one.** Vitest browser mode pre-declares deps; an unlisted one is discovered mid-run, triggers a re-optimization and reloads the page under the tests. **Each subpath is its own entry** — `@charcuterie/logic` does not cover `/query` or `/openapi`. The query fan-out added `logic/query` imports to seven repos and updated **no** list; nothing broke because an unlisted entry is a *race*, not a determinate break. mux-magic lost it two days later when the OpenAPI split added a second unlisted subpath: **16 tests, across a set of files that differed between runs** (the tell), with the real cause two levels down — `Failed to fetch dynamically imported module: …?v=4aa5959c`, the changing hash being the re-optimization signature, reported through a `beforeEach` frame. Audits found the lists short by **3** (mux-magic) and **4** (rip-deck) entries. Source of truth is the repo's own `_metadata.json` after a **cold** run, not a sibling's list hand-filtered (which is how rip-deck's drifted) — and the cache lives under `packages/<app>/`, so clearing the repo root leaves it warm and passing. **Five more adopters are unaudited.** |
| 2026-08-13 | [The OpenAPI seam is its own subpath, not part of `./query`](2026-08-13-the-openapi-seam-is-its-own-subpath-not-part-of-query.md) | **An optional peer that everyone has to install is not optional.** [The 08-11 data-fetching decision](2026-08-11-charcuterie-owns-data-fetching-via-query.md) put the client, the provider *and* the `openapi-fetch`/`openapi-react-query` re-exports behind one `./query` barrel, on the theory that optional peers meant a non-OpenAPI app pulled none of them. The seven-repo adoption fan-out disproved it: the barrel re-exports `createApiClient.ts`, whose **types** reference both libraries, and TypeScript resolves the whole barrel to typecheck any import from it — so the **six of seven apps with no OpenAPI document could not typecheck a bare `QueryProvider`** without installing both. rip-deck shipped the workaround live ([#30](https://forgejo.octen.dev/sawtaytoes/rip-deck/pulls/30)): two dependencies in `package.json` that its source never imports. Split into `./query` (`createQueryClient`, `DEFAULT_QUERY_OPTIONS`, `QueryProvider` — needs only `@tanstack/react-query`) and **`./openapi`** (the typed seam and its types). Named for the library it binds, like the existing `./jotai` and `./signals` adapters — a subpath *is* how this package says what a thing costs. Breaking, so `@charcuterie/logic@2.0.0`; taken now rather than as a deprecated re-export precisely because **one** repo imports the moved names today (mux-magic, a one-line change) and because a re-export would preserve the type-graph problem it exists to fix. |
| 2026-08-13 | [`Picker` is the assembled `Listbox`; `Listbox` stays trigger-agnostic](2026-08-13-picker-is-the-assembled-listbox-and-listbox-stays-trigger-agnostic.md) | **Four independent implementations of the same thirty lines, and the fourth is this package.** After [the 08-10 demotion](2026-08-10-listbox-and-combobox-are-the-default-and-select-is-demoted.md) made `Listbox` the default picker, every migrating app had to assemble the same trigger — button showing the current value, chevron, open state — and none shared it: queuepilot's `SelectListbox`, board-games' `SelectMenu` (on `useState`, not the state layer), mux-magic's `ListboxPicker`, and **twice inside `@charcuterie/ui` itself** (`QueryBuilderCombinator` and `QueryBuilder`'s story), each with its own hand-rolled chevron. `Listbox` is **unchanged** — trigger-agnostic is what lets it hang off a tile or a table header — and `Picker` is the assembled default beside it, the same base-layer/component split as [`Modal`/`Dialog`](2026-08-03-modal-is-the-base-layer-and-dialog-is-the-component.md). The duplication was not uniform and **the differences were bugs**: queuepilot set `aria-label={label}` alone, so the accessible name did not contain the trigger's visible text — a **WCAG 2.5.3 failure four reviews missed**; `Picker` names it `"<label>: <value>"`, which also makes each control uniquely findable. Documents the trap queuepilot paid for: `useAnchoredOverlay` **overwrites the trigger's `id`**, so `id` is silently replaced and `data-testid` is the stable e2e handle. Named `Picker` by the owner over `SelectListbox`/`ListboxButton`, with the `useSinglePicker` collision raised first and answered in the record. Component count 44 → 45; the two app-side copies are left for those repos to take deliberately. |
| 2026-08-11 | [Shared app logic and helpers unify into Charcuterie](2026-08-11-unify-app-shared-logic-into-charcuterie.md) | **The umbrella direction: Charcuterie is the home for shared *logic*, not just design.** Capabilities each app re-solves — data-fetching, helpers, cross-cutting hooks — move into a Charcuterie package, edited once and inherited everywhere, so apps get thinner and are maintainable with agents. Guardrails: **prefer an existing package** over a new one (a new publish needs the owner's manual OIDC seed); **one concern per package** (`query` ≠ `streams` ≠ a future router); **re-export a good primitive, don't reimplement it**; **bake in learned defaults, keep them overridable**; **record each move as its own decision**. Owner: *"I want to unify everything. Helpers, logic, etc … It'll make updates simpler and make it easier to build apps and for me to maintain them with agents."* |
| 2026-08-11 | [Charcuterie owns request/response data-fetching, via `@charcuterie/logic/query`](2026-08-11-charcuterie-owns-data-fetching-via-query.md) | **Fetching gets a Charcuterie home — a new `./query` subpath on the already-published `@charcuterie/logic`** (a subpath, not a new package, so no manual first publish). `createQueryClient` keeps react-query's own defaults (**retries stay on** — the polling apps opt *out* with `retry: false`, rather than baking one biased sample's preference in fleet-wide) and deep-merges overrides; `QueryProvider` wires it; `createApiClient`/`createApiHooks` re-export `openapi-fetch`/`openapi-react-query` at full type fidelity (all three libs are **optional** peers). **Revises one row** of the 2026-07-31 `streams` decision (the "fetching is the consumer's react-query" line); the rest of that decision — `streams` is push-only RxJS and a *separate* package — stands. Not a re-implementation (re-export only), so the old "second retry answer" worry never arises. |
| 2026-08-11 | [Typed API calls use `openapi-typescript`; the generated schema is committed but lint-ignored](2026-08-11-typed-api-calls-via-openapi-typescript-generated-schemas-committed.md) | **Type-safe calls come from the backend's OpenAPI doc, not hand-typed contracts.** `openapi-typescript` generates a `paths` type to a canonical `__generated__/api.gen.ts`, **committed** (resolves on a fresh clone, CI typechecks what shipped, schema changes diff in review) but **excluded from Biome + ESLint centrally** — `@charcuterie/biome-config` disables lint/format/assist for `**/*.gen.ts` + `**/__generated__/**`, and `@charcuterie/eslint-config` exports `createGeneratedIgnores()`/`GENERATED_SCHEMA_GLOBS`. House rules have no authority over machine output; type-aware linting a 10k-line type is cost for no signal. Survey found only mux-magic generating types (and its web still hand-rolled `fetch`); everyone else hand-typed and drifted. Verified: a bad `probe.gen.ts` is reported ignored by both tools. |
| 2026-08-11 | [Comments in `index.html` must not shadow Vite's injection anchors](2026-08-11-index-html-comments-must-not-shadow-vites-injection-anchors.md) | **A blank dev page that no CI job can see, shipped twice in six days.** Vite finds its script-injection points with comment-blind regexes (`/<head[^>]*>/i` and siblings), so the first literal `<head>` in the file wins even inside `<!-- … -->` — and `@vitejs/plugin-react`'s preamble plus `/@vite/client` land in the comment, where the browser never parses them. `$RefreshReg$` undefined, HMR dead, page blank; **production builds are unaffected, so `typecheck`/`lint`/`unit-tests`/`e2e`/`build-budget`/`storybook-build` all stay green**. image-viewer solved it on 2026-08-05 in a **Locked** record that reached nobody, and mux-magic hit the identical bug on 2026-08-11 ([#200](https://github.com/Sawtaytoes/mux-magic/issues/200)). Now enforced by `@charcuterie/vite-config`'s base — **not opt-in**, throws in `serve` *and* `build`, from `configResolved` (so `yarn dev` dies at startup, not on first request) and `transformIndexHtml`. **Not an ESLint rule**: ESLint does not read HTML, and a script nobody wires up is not a mechanism. Narrowed from image-viewer's blanket ban to *"shadows the real tag"*, because mux-magic's shipped fix keeps the word `<head>` in a comment now sitting **inside** `<head>` — that file is correct. Open `<html>` is not an anchor. Verified against a real dev server on all four before/after fixtures; the fleet sweep found rip-deck carrying a latent `<body>` at line 62. |
| 2026-08-11 | [A flex row's text child must declare how it shrinks](2026-08-11-a-flex-rows-text-child-must-declare-how-it-shrinks.md) | **Four independent rediscoveries of one CSS rule in one day.** A flex item's automatic minimum size resolves against its content's **min-content width**, so one unbreakable token — a URI, a host, an item name — becomes the row's floor and shoves its sibling out of the container; the [17px ramp](2026-08-10-the-type-ramp-is-built-around-a-17px-body.md) consumed the slack that had been hiding it in gallery-downloader, points-market, mail-sifter and rip-deck on the same day. `min-w-0` alone is **not** enough (the text spills instead) — only `overflow-wrap: anywhere` shrinks the min-content size — and the four shipped fixes were four *different* fixes, which is why the rule flags the dangerous shape and accepts any escape. `charcuterie/no-unconstrained-flex-text` **warns** because it cannot know whether `{status}` is `"OK"` or a 300-character URL, and a rule that fires constantly gets disabled; `charcuterie/no-shrink-0-with-flex-wrap` **errors** because the two classes contradict outright — it found the rip-deck bug plus two uncaught siblings on its first run. `<div>`, static text, `tabular-nums`, column containers and unreadable classNames are all out of scope, each for a measured reason. mail-sifter's host is a grid block, not a flex item, and is deliberately not caught. |
| 2026-08-10 | [`content.muted` is strengthened so the highlighted option row clears AA](2026-08-10-content-muted-is-strengthened-so-the-highlighted-option-row-clears-aa.md) | **Closes the hole [#72 recorded and left open](2026-08-10-interactive-states-are-audited-not-just-resting-states.md), and finds it was three times bigger than reported.** `content.muted` on `intent.neutral.surfaceHover` — the highlighted row in `Listbox`/`Combobox`/`Menu`, the components the fleet is [actively being migrated onto](2026-08-10-listbox-and-combobox-are-the-default-and-select-is-demoted.md) — failed AA in four of eight combinations. Extending the gate **structurally** instead of adding the one named pair surfaced **12 failures, not 4**: the *selected* row (`intent.accent.surface`) fails too, and `content.muted` on **`surface.sunken`** fails **at rest** in hairline/light at **4.34**, a plain non-interactive surface failing since M0 that nobody had seen because the surfaces block hand-listed `["base", "raised", "overlay"]`. Diagnosis by elimination, not taste: across all six intents, both tint states and all eight combinations **every failing pair is `content.muted`** — `primary` and `secondary` clear everywhere unchanged (secondary's worst is 5.68), so it is a weak foreground, not a too-close `surfaceHover` and not a misclassified label (`ListboxOption`/`ComboboxOption` already paint labels `text-content-primary`; the only muted text in a row is a genuine secondary detail line). `content.muted` moves in **six of eight** combinations (daylight `#616A7C`→`#565E6D` / `#8B94A5`→`#99A1B0`, hairline `#686D74`→`#5D6168` / `#838991`→`#8E949B`, layered `#676274`→`#605B6C` / `#9A95AB`→`#9B96AB`); **`legible` and ePaper untouched**. The **4.8:1 bar is derived, not invented** — `legible` already clears it everywhere with no change — and nothing lands on 4.50, where `hairline`'s dark `danger.solid` sits. **No tint moves**: the [2026-08-05 highlight mechanism](2026-08-05-option-rows-carry-no-base-bg-transparent-and-highlight-with-surface-hover.md) is untouched, not superseded. Structural half: every role list in `contrastAudit.ts` is now derived from its union (`CONTENT_ROLE_AUDIT`, `SURFACE_ROLE_CARRIES_CONTENT`, `INTENT_TINT_CARRIES_PLAIN_CONTENT`, `INTENT_ROLE_IS_TINT_BACKGROUND`), so a new content role, surface role or intent will not compile until classified and fails a test until measured — **48 gated pairs per scheme becomes 63**. Axe gap was worse than #72's `ButtonLink` miss: `Listbox.test.tsx` **never mounted `AllVariants` at all**, the only story with `text-content-muted` in a row, so the pair had zero axe coverage in any state; the new test drives a row to highlighted, **asserts the fill is really painted** (else it would pass on a resting row — #72's exact trap), and fails with `color-contrast (serious)` in real chromium on the old values. |
| 2026-08-10 | [Interactive states are audited, not just resting states](2026-08-10-interactive-states-are-audited-not-just-resting-states.md) | **The gate could not see a whole class of state, and reported that as a pass.** `contrastAudit.ts` contained **zero occurrences of "hover"** since M0: it measured `onSolid` on `solid` and intent `content` on `surface`, never on `solidHover`/`surfaceHover`. So `yarn check:contrast` printed *"All variants clear WCAG 2.2 AA"* for the library's whole life while `daylight`'s dark `intent.accent.solidHover` (`#6A64F0`) sat at **4.47:1** against its white label — and `daylight` is the `:root` default, so **every accent button in the fleet failed AA while hovered**. It surfaced only as a side effect of [#68's post-mount settle](2026-08-10-automated-suites-freeze-motion-fonts-and-post-mount-effects.md), which made an intermittent axe `color-contrast` flake on `ButtonLink` deterministic; without that it would have stayed something people retried past. Every resting pair now has its hover twin at the same threshold — **35 gated pairs per scheme becomes 48** — in the file's existing `label` convention. Three fills failed and were fixed **at the hover token**, never the resting colour or the foreground: `daylight/dark` accent 4.47 → 6.19, `hairline/dark` accent 3.91 → 5.59, `hairline/dark` danger 3.83 → 5.37. The rule they now follow is a description of what every light scheme here already did: **a hover moves away from its own label's lightness** — white-label fills deepen, dark-label fills brighten — so the hovered state is *more* readable than the resting one. All three land 0.8–1.7 above the line rather than on it, because a value at exactly the threshold re-breaks on any rounding change (which is where `hairline`'s dark `danger.solid` sits today, at **4.50:1**). The structural half is the actual fix: `RESTING_ROLE_BY_INTENT_ROLE` is keyed by every member of `IntentRole`, so a future `solidPressed` **will not compile** until it is classified and then fails a test until `auditScheme` pairs it with a foreground. Records one hole left deliberately open: `content.muted` on `intent.neutral.surfaceHover` (the `Listbox`/`Combobox`/`Menu` row highlight) fails in four of eight variant × scheme combinations and cannot be fixed from the hover token — it needs a resting foreground moved, in its own change. ePaper is untouched, [exempt by its own decision](2026-07-31-epaper-is-exempt-from-the-contrast-gate.md). |
| 2026-08-10 | [The app shell is `Shell` / `Header` / `Rail` / `Main`, with one width and a real sticky](2026-08-10-the-app-shell-is-shell-header-rail-main.md) | **The fleet's largest duplicated surface, owned once.** Ten of twelve UI repos hand-roll the page chrome; three have a file named `AppShell.tsx`, and mail-sifter's and points-market's header elements are a **byte-identical** class string arrived at independently. Owner: *"We need a header component... there's no standardized way to do a sticky header + narrow view... A Unified App Shell basically."* **Bare nouns**, flat from the barrel, matching `Button`/`Card` — not `AppShell`/`PageHeader`, not compound `Shell.Header`; `Rail` takes `side` rather than splitting in two; booleans keep the `is` prefix, so `isSticky`. **`isSticky` writes `position` AND `z-index` together** from `--layer-sticky`, because mux-magic's `PageHeader.tsx:173` is documented as sticky and sets only the z-index — it scrolls away, valid CSS, no error. **`contentWidth` is stated once on `Shell`** and read by `Header` and `Main` through context: points-market ships those two disagreeing (header 80rem, `<main>` uncapped). It takes a `screen.*` step (default `lg`), `"full"`, or a `` `${number}rem` `` literal — the last exists so rip-deck's `contentMaxWidthRem(columns)` composes with no import in either direction. `max-w-*` is refused (the `--container-*` collision). Rails collapse **by restyling one element**, never a second copy behind `hidden`/`lg:hidden` (mux-magic and mail-sifter both ship that; every control lands in the DOM twice at every viewport). Gated at a real 390px chromium: `documentElement.scrollWidth <= clientWidth` against a 130-character unbroken path and an over-wide table, each separately asserted to overflow. |
| 2026-08-10 | [The automated suites freeze motion, wait for fonts, and settle post-mount effects](2026-08-10-automated-suites-freeze-motion-fonts-and-post-mount-effects.md) | **Owner's ask, and the fix for CI flake that five local runs could never reproduce.** *"We should be able to disable animations in Storybook for the purposes of running VRTs and always when running tests."* Three `ui-dom` tests had failed intermittently in CI — `ButtonLink`'s `color-contrast` (4.47 vs 4.5 at `#6A64F0`, which **is** the daylight `solidHover`), `Toast`'s naming, `Dialog`'s `scrollable-region-focusable` — while passing 5/5 locally. Root finding: **`vrtCapture.mjs` already had all three mitigations and `ui-dom` had none**; that asymmetry was the bug. Motion/caret/scroll freeze now ships once at `@charcuterie/storybook-config/testing` (its own subpath, so it never reaches a browser bundle) and is used by both suites; `ui-dom` gains `await document.fonts.ready` (fallback metrics decide whether a body overflows its clamp) and `mountStory` gains a one-frame settle (`storybook-addon-pseudo-states` applies forced `:hover` **a tick after render**, and the a11y addon audits the instant `run()` resolves — so a contrast check reads `solid` on one run and `solidHover` on the next). Chose an **override stylesheet over emulating `prefers-reduced-motion`**: the media query is a real code path here, but using it would test the reduced-motion rendering nobody reviews and re-baseline every VRT shot with a looping affordance. Two hypotheses tested and **refuted** with probes, recorded so they are not re-proposed: `page.viewport()` does *not* resize the shared window (it posts an `iframeId` and resizes that iframe; only the 3 calling files ever left 414px), and no stale `:hover` was present at test start. The settle's first draft broke `Toast` into 15s timeouts because that file **stubs `requestAnimationFrame` and holds the callbacks** — it now binds the native rAF at module load. 5/5 green at unchanged 11–12s. Dev Storybook verified unaffected: `charcuterie-freeze-motion` appears zero times in `storybook-static`. |
| 2026-08-10 | [The type ramp is built around a 17px body, and `sm` is pinned to 1rem](2026-08-10-the-type-ramp-is-built-around-a-17px-body.md) | **Owner's pick from four served candidates, after reporting the whole fleet reads "SUPER small".** The ramp becomes 15 · 16 · **17** · 19 · 24 · 30px for every variant (`layered` keeps only its larger *display* steps, 25/32); line height `1.28/1.55/1.7`. The old default ran 12 · 13 · **15** · 17 · 21 · 26 and reached 16px only at `lg` — Charcuterie's `md` was Tailwind's `text-sm`, which is why he ran these apps at **200%** zoom against **125%** for Home Assistant. The step that decided the shape is **`sm`, not `md`**: `text-sm` is used **34 times** in `packages/ui/src` against `text-md`'s **11**, so `sm` is the library's real body step and is pinned to exactly 1rem. Three `text-xs` groups are reclassified to `text-sm` in the same PR because they are content, not fine print — the whole **Tooltip** body, **Field**/**FieldGroup** descriptions *and errors*, and the `uppercase tracking-wide` **SortableTableHeader** label. **Control heights deliberately unchanged** (the button already read fine; height was settled 08-05). Contrast gate is size-blind so it is unaffected either way; `vrt` diffs ~356 shots by design; `AdaptiveGrid`'s `itemBlockSize` goes 16px stale (147px → 163px card). Retires the "body copy at 13px" premise in the fonts decision. |
| 2026-08-10 | [Buttons are for on-page actions, links are for navigation — and there are two link components](2026-08-10-buttons-are-actions-links-are-navigation.md) | **Owner's rule, encoded in the API rather than in a doc.** A control that navigates is an `<a href>` (middle-click, ctrl-click, open-in-new-tab, copy-link-address and the status bar cannot be added to a `<button>` afterwards); a control that changes the page is a `<button>`. The library ships **`TextLink` and `ButtonLink`, not one `Link` with an `appearance` prop** — both render a real anchor and differ in **paint, not semantics**, and an agent choosing between two names makes a clearer decision than one setting a prop. `Button` grows no `href` and no `asChild`. `ButtonLink` shares `Button`'s maps through `getControlClassName` (computed styles compared in-browser, not class strings copied); **no `isLoading`** — a navigation has no pending state the component owns. A disabled link **drops `href`** (the platform's own inertness) and sets `aria-disabled` + `role="link"`, never a focusable anchor whose handler returns early. Fixes both shipped failure directions: plex-channels navigating with `<button onClick={navigate}>`, and mux-magic's 14 raw `<a href>` reloading the page through its own SPA router. |
| 2026-08-10 | [Columns are bought with height, and the content cap widens to pay for them](2026-08-10-columns-are-bought-with-height-not-width.md) | **Owner's ask, on the whole fleet: *"1 column, but waaaaaaay too wide"* and *"not make it ugly where you have 7 items across a super wide monitor."*** `AdaptiveGrid`/`useAdaptiveColumns` add a column only when the items will **not** stack inside the viewport (`ceil(itemCount / rowsThatFit)`); the container's inline size can only ever **cap** that, never produce it — the inversion of the `auto-fill, minmax()` every other grid in the fleet uses. Visible consequence is deliberately non-monotonic and the thing most likely to be "fixed" by someone who hasn't read this: **1440x900 takes three columns while the larger 1920x1080 takes two**, because the taller window stacks the same items in fewer stacks. The cap around the grid then **widens with the count** (1→56rem, 2→72rem, 3→106rem, 4→140rem), so a page earns its width by having something to fill it with; the three numbers land in `@charcuterie/tokens` as `contentInlineSize` beside `screen`/`containerQuery` (a readable measure is a structural fact, not taste), the arithmetic stays in `ui`. **The two threshold systems are reconciled by keeping them apart** — a `cq-*` step is read by the item *inside* the track, the grid's floor by the grid *outside* it — but the floor moves onto Charcuterie's scale, defaulting to `containerQuery.sm` (384px) against rip-deck's hand-measured 380px, and a test proves all eleven spec sizes answer identically at either value, so it's a rename not a behaviour change. Inline size off a `ResizeObserver` on a **container**, block size from the **viewport** behind an injected resolver (a grid in normal flow is always exactly as tall as its contents, so it would always answer "it fits"). Ships as two boxes because collapsing them is a silent bug: cap the element you measure and the fold reads back its own narrowed output — one column forever, no error anywhere. |
| 2026-08-10 | [`Listbox` and `Combobox` are the default; `Select` is demoted](2026-08-10-listbox-and-combobox-are-the-default-and-select-is-demoted.md) | **Supersedes the 2026-08-03 siblings record, on the owner's account of where it came from** — *"I believe **you** came up with that, not me, and it was too much trouble at the time to convert all the apps. But now that we have them, I wanna change that."* Every fact the old record cited is still true (wheel picker, autofill, `:invalid`, no-JS form submission); the **conclusion** was drawn while `Listbox`/`Combobox` were still being built, when "stays first-class" was indistinguishable from "has no replacement yet". Now: `Listbox` (short, rich) and `Combobox` (long, searchable) are the default, `Select` is **demoted, not deleted** — still exported and still right for those four platform cases, but each call site says which in a one-line `-- reason` on the disable comment. Enforced by `charcuterie/prefer-listbox-over-select` + `charcuterie/require-suppression-reason` in the opt-in component-choice block, next to `charcuterie/no-raw-select` for the **134 raw `<select>` in bambuddy**, 19 in spoolbuddy and 2 in points-market that never reached a charcuterie component at all. **Deliberately not built:** native-on-touch / `Listbox`-on-desktop, which the owner raised as a real possibility for later — recorded so nobody ships it by accident. Two ARIA trees behind one name doubles every story, test and axe run, the `ReactNode` options cannot survive the native branch, and a wrong capability guess is worse than either fixed choice. |
| 2026-08-05 | [`master` merges require all four CI checks, squash-only, no bypass](2026-08-05-master-requires-all-four-ci-checks-no-bypass.md) | **`master` had no protection, which is how #39 merged with a red `lint` check.** A GitHub ruleset (`Master`, active) now requires `lint` + `typecheck` + `test` + `storybook` (the `ci.yml` jobs), a PR, `squash`-only merges, and linear history, with **`bypass_actors: []`** — a red check blocks the merge for everyone including the AI's token. Owner chose **all four** checks (not lint-only — a failing test shouldn't merge) and **strict, no bypass** ("gate" means block). Corrects the assumption that mux-magic gated on lint: mux-magic's ruleset requires PR + linear history + GitHub's `code_quality` rule, **not** the CI status checks, so this was never wired anywhere. `code_quality` omitted here (it's code-scanning, not configured). Safe from the stuck-check trap — `ci.yml` runs on every `pull_request` with no path filter. |
| 2026-08-05 | [`Menu`'s `items` is a discriminated union, not a flat item list](2026-08-05-menu-items-is-a-discriminated-union.md) | **Owner chose the union over a smaller step or a maximal one.** `items` becomes `MenuEntry[]` = `MenuItem \| MenuSeparator \| MenuGroup`: a separator (`role="separator"`) and a group (`role="group"`, named by its label) opt in by `type`, a bare item still type-checks, plus an `emptyState?` prop. The keyboard model needs **no new code** — separators and group headings register nothing, so the arrow keys skip them the way a disabled item already is, and a group's items register normally (focus walks them in DOM order). Backward compatible. **Arbitrary interactive nodes excluded on purpose** — a focusable custom node in a `role="menu"` breaks the roving-tabindex model and the menu/listbox role contract, the one variant that can't be added without redesigning focus. The **empty state is a disabled `menuitem`, not inert text**: a `role="menu"` with no `menuitem` child fails `aria-required-children`, so the note is one (`aria-disabled`, `tabindex="-1"`, announced but never focused). `Grouped` + `Empty` stories/tests, axe clean. |
| 2026-08-05 | [`Field` adopts the child control's own `id`; the `Field` prop wins when both are set](2026-08-05-field-adopts-the-childs-own-id.md) | **Owner's call on the "both set" case the backlog flagged.** `Field` cloned a minted `<baseId>-control` id onto its child and honoured an `id` passed to the *Field*, but a control written `<input id="rename-pattern">` had its own id overwritten — the label/control pair still agreed, but every outside-in reference (deep link, autofill hint, server-rendered error summary, a consumer's selector) broke. New precedence: `<Field id>` → child's own `id` → generated; the **Field prop wins** when both are set (outer/later declaration, last-writer-wins for a value). "Child always wins" rejected (flips documented behaviour); a conflict warning rejected (noise for a defined case). Non-breaking — only the child-had-an-id-and-Field-didn't case changes (previously lost, now honoured). `AdoptsChildId` story + test, axe clean. |
| 2026-08-05 | [Neutral ghost-hover deepening swept to `layered` and `hairline`](2026-08-05-neutral-ghost-hover-swept-to-layered-and-hairline.md) | **Owner-approved follow-up to the daylight fix, reviewing a served before/after.** Measuring all four light variants' neutral `surface` against their own base settled which of the rest were weak: `layered` ΔE 11.1 (the weakest of all four) and `hairline` ΔE 15.3 both got the same deepening (`layered` `#EDEAF0`→`#E8E4EC` + `surfaceHover` `#E4E0E9`→`#DFDBE7`; `hairline` `#EFECE6`→`#ECE8E1`, its `surfaceHover` already deep enough). **`legible` is left untouched** — already ΔE≈23, on par with the chromatic intents. Dark schemes untouched everywhere. Deepen-the-token (not repoint-the-role) for the same reasons as daylight; done per-variant by the numbers, not blanket, so a variant that was already fine is not disturbed. Content-on-surface stays 8.2–8.4:1; all variants clear WCAG 2.2 AA. |
| 2026-08-05 | [daylight/light neutral surface is deepened so a neutral ghost hover reads](2026-08-05-daylight-neutral-surface-deepened-for-ghost-hover.md) | **Owner's call, reviewing a served before/after — *"I love it!"*** A `ghost` neutral button (toolbars/icon rows) is transparent until hovered, then lands on `intent-neutral-surface`; in daylight/light that surface `#EDF0F5` was only ΔE≈12 from the page base, so the hover was nearly invisible — the exact thing flagged when the scheme switchers shipped (dark already read; focus ring + name + axe passed regardless, so nothing automated saw it). The residual is specific to **neutral**, not `ghost`: the chromatic intents read at the `surface` step (accent is ΔE≈21) because their surface is a tint; neutral's is grey-on-near-white. Chosen fix: **deepen the neutral surface token** (`surface` `#EDF0F5`→`#E6EBF2` ΔE≈21, `surfaceHover` `#E3E8EF`→`#DEE4EF`) rather than repoint neutral's hover role (keeps `ghost`→`surface` uniform across all six intents) or add per-app touches (rejected — against the library's point). Ramp stays monotonic (base 0 → surface 20.8 → surfaceHover 31.8 → border 35.7) and `surfaceHover` stays lighter than the border so `soft`'s outline survives hover. Neutral content on the new surface 8.29:1; all variants clear WCAG 2.2 AA; 137/137 tokens tests pass. daylight only — the other light variants are untouched for now. |
| 2026-08-05 | [The Storybook setup is a published package, not a copied `.storybook/`](2026-08-05-storybook-config-is-a-shared-package.md) | **`@charcuterie/storybook-config` — the sixth published package.** Four consumers (`docs`, `gallery-downloader`, `rip-deck`, and `mux-magic` via `addon-themes`) had hand-copied the theme-axis writer + `themeAxes` table, trimmed to three different axis subsets; the copies had already drifted. The setup — the `scheme`/`variant`/`density` toolbars and their one `SET_GLOBALS`/`GLOBALS_UPDATED` writer, the first-paint `previewHead` seed, the a11y + controls params, the GFM docs addon, the Tailwind `viteFinal`, and the React-Aria focus preload (so **not** `sideEffects:false`) — moves into one package with the axis subset and a11y stance as arguments (`installThemeAxes(keys)`, `themeParameters({ isA11yEnforced })`). Three entries keep Node/browser/vite apart. First publish is manual (OIDC can't seed a nonexistent package); `docs` is the dogfood consumer. See also [how-we-do-storybook](../how-we-do-storybook.md). |
| 2026-08-05 | [Combobox multi-select is `isMultiple`, not a separate component](2026-08-05-combobox-multi-select-stays-ismultiple-not-a-separate-component.md) | **Owner's call, reviewing the multi-select in Storybook** — the chips rendered *inside the popup* and vanished on close ("no tag … no way to remove them"). Multi-select stays a **mode of `Combobox`** (`isMultiple`) with an always-visible row of removable chips (human label + ✕) above the trigger; no `MultiSelectField`/`TokenInput` is added because the family is named by **ARIA role** and a tag-multiselect has none — it is still a combobox. The two shapes share filtering/virtualization/keyboarding; only the chip row differs, so it is a mode, not a component (extractable later if ever wanted). mux-magic's `LanguageCodesField` already proves the UX by hand-rolling it; this folds it into the mode. |
| 2026-08-05 | [Option rows carry no base `bg-transparent`, and highlight with `-surface-hover`](2026-08-05-option-rows-carry-no-base-bg-transparent-and-highlight-with-surface-hover.md) | **"The arrow keys don't work" — they did; the highlight was invisible.** Two stacked bugs: (1) the row's base `bg-transparent` is a plain `background-color` utility at the same specificity as the `isActive`/`isSelected` tints, and Tailwind emits it **last**, so it won every row — the Combobox keyboard cursor and Listbox's selected accent rendered with no fill (measured: `class="bg-transparent bg-intent-neutral-surface"` computed `rgba(0,0,0,0)`). (2) The highlight used `intent-neutral-surface`, a *base-surface* tint that is **darker than `surface-overlay` in every dark scheme**, so even fixed it read as no change. Fix: drop the base `bg-transparent` (a button is transparent by default) and highlight with `intent-neutral-surface-hover`. Verified live; 355 `ui` tests pass. |
| 2026-08-05 | [Controls share one height — no per-component touch-target floor](2026-08-05-controls-share-one-height-no-per-component-touch-floor.md) | **`Select` was 44px while `Button` was 40px at `md`** because `Select` alone applied `MIN_TOUCH_TARGET_CLASS` on top of the shared `CONTROL_SIZE_CLASS`. Height must come from the control-size/density system **only** — controls that share a form row must match at the same `size`. Touch is the **density axis's** job (`[data-density]` scales `--control-height-*` past 44px on a kiosk), so the per-component 44px floor is redundant on touch and wrong on desktop. 44px is WCAG **2.1 AAA** (SC 2.5.5), not an AA requirement (2.2 AA is 24×24). Fix: **drop the floor from `Select`** (non-breaking — shrinks Select 4px to match, vs growing every `md` button). `MIN_TOUCH_TARGET_CLASS` stays an opt-in for standalone tap targets only. |
| 2026-08-05 | [Dank Mono reaches an app by a read-only bind mount, not a font server and not the repo](2026-08-05-dank-mono-reaches-the-fleet-by-bind-mount.md) | **How the file gets in — the piece the Victor Mono record left open.** [That record](2026-07-30-the-shipped-mono-is-victor-mono.md) said consumers override `--font-mono` "from their own licensed copy" but its snippet assumes the woff2 is already in the app's `public/`, which for most of this fleet it must never be: the purchased EULA grants use-on-websites but forbids **copying/redistribution**, and half the consumers are **public GitHub** (`castkit`, `image-viewer`, `mux-magic`, `plex-channels` — `gh`-confirmed), so committing the file is redistribution and a private-only carve-out renders the fleet inconsistently. The file therefore reaches an app **without passing through git or CI at all**: a **read-only bind mount** of the NAS archive (`/mnt/Bunnies/Kevin/Apps/Fonts/Development/DankMono/Web-PS`) onto the app's static root at `app.create` time — makes **zero copies** (sidesteps the copying clause), needs no off-LAN build access (GitHub runners can't reach the NAS anyway), and adds **no standing service**. Rejected: committing it (redistribution), baking into the image (per-build copy + off-LAN), and a shared `fonts.octen.dev` — *"we're not tying my personal font server to this just for Dank Mono. Mounting a volume is something any app can do easily."* **image-viewer is the one exception**: an offline EXE with nothing to mount gets `local("Dank Mono")` only, no `url()`. Victor Mono stays the fallback in every `--font-mono`. |
| 2026-08-03 | [Overlays portal to the body, not the top layer](2026-08-03-overlays-portal-to-the-body-not-the-top-layer.md) | **Supersedes the 2026-07-30 top-layer record — and that record reserved the reversal in its own last paragraph.** The top layer wins `z-index` and paint order but is still laid out in place, so a `transform`/`overflow: hidden` ancestor still clips a `position: fixed` panel; a `FloatingPortal` to `document.body` has none. The fleet hit it repeatedly (mux-magic's `PortalDropdown` reached for `createPortal`; `PathPicker`-behind-`EditVariablesModal` was a logged clipping bug axe cannot see). The old objection — a portal moves the node out of a scoped query — is **answered, not waved**: `useRole` writes `aria-controls`/`aria-labelledby` across the boundary (tests assert `trigger.aria-controls === panel.id`), and `expectAgentDrivable` is structural so it needs no change — only the stories scope panels to `body`. Two wins fall out: the M4 synthetic-Escape limitation dies (`useDismiss` listens for a real keydown), and one shared scrim + top-first dismissal replaces N stacked `<dialog>`s. `Toast` is exempt. |
| 2026-08-03 | [`Modal` is the base layer; `Dialog` is the chrome component](2026-08-03-modal-is-the-base-layer-and-dialog-is-the-component.md) | **The name was wrong in the way that matters.** `Modal` was a *dialog* — heading, Close, footer, five sizes — so by taking the generic name for the specific thing the library had no generic backdrop-plus-dismiss layer for everything else (a confirmation, `Lightbox`'s media view, a bare overlay). The old chrome renames to `Dialog` verbatim (`xl`, the `full` `rounded-none`, all of it); a new base `Modal` renders `OverlayPanel` and no chrome. `Lightbox` — written as a "skin over `Modal`" that used the chrome only incidentally — is the proof, and migrates to `Dialog` with no behaviour change. Breaking (`ui@2.0.0`): three consumers rename `Modal`→`Dialog`, props unchanged; `ModalSize`→`DialogSize`. The one Biome suppression in the package goes with it — the backdrop is a real element now, dismissal is `useDismiss`. |
| 2026-08-03 | [`Listbox` and `Combobox` are siblings of `Select`, not replacements](2026-08-03-listbox-and-combobox-are-siblings-of-select.md) | **Native `Select` stays** — it keeps type-ahead, Home/End, the mobile wheel, form submission, `:invalid`, autofill. `Listbox`/`Combobox` exist only for what `<option>` cannot render (an icon, two lines, a badge), and retire the "component with no caller" refusal: `Combobox` has seven consumers waiting. The ARIA split is forced, not stylistic — `Listbox` roves focus onto its options (APG type-ahead, `aria-selected`, a button trigger that is *not* a combobox); `Combobox` keeps focus in the input and tracks the active row with `aria-activedescendant` over zero-tab-stop options. Virtualization takes `@tanstack/react-virtual` (the package's second runtime dep — MIT, US-origin, ~4 KB gz, clears the provenance rule); windowed rows carry `aria-setsize`/`aria-posinset` or a reader announces "2 of 12" for a 4,000-item list. `TypePicker` stays a `Menu`. |
| 2026-08-03 | [The theming axes are written at preview bootstrap, not by story decorators](2026-08-03-the-theme-axes-are-written-at-preview-bootstrap.md) | **Two bugs the composed site exposed, and one of them CI could not see.** Every axis was written by a story *decorator* — including `withThemeByDataAttribute`, which is one too — so `Tokens/Overview`, unattached prose with no `<Canvas>` on it, never got any. And a missing attribute is **not "light scheme", it is no theme at all**: `variables.css` keys every `--color-*` off `[data-scheme]` with no `:root` fallback, so measured cold the page had **all three attributes absent** and `--color-surface-base` resolved to the **empty string**, falling through to stock Storybook white. The preview document is shared, so clicking any story stamped them and nothing removed them — going *back* looked right, which is why it read as "loads in light mode until you click a story". Now: a `previewHead` seed, a module-scope `SET_GLOBALS`/`GLOBALS_UPDATED` subscription, and a decorator kept **only** for the channel-less renders (`composeStories` under `test:ui-dom`, the one suite that asserts density-derived sizes) — all calling one writer off one table, because `main.ts` is Node and `preview.tsx` is the browser and a seed that disagrees with `initialGlobals` reads as a flash rather than a bug. A seed alone would have been the *visible* half and left the toolbar dead on Overview. Separately, MDX prose tables were never covered by anything: `tokens.css` set `border-color` and stopped, so ten pages painted `rgb(46, 51, 56)` cells at **1.1:1** from Storybook's stock light `Table`. No component could have fixed it — a GFM table is a plain `<table>` and the library ships `SortableTableHeader` and nothing else. **Why CI missed it:** the smoke gate's one `goto` lands on a *story*, so a decorator had always already run — the cold case was structurally unobservable, and it is the **normal** case on `storybook.octen.dev`, which deep-links straight at a docs path. |
| 2026-08-02 | [`useClonedChild` keeps `cloneElement` — the replacement is deferred, not rejected](2026-08-02-useclonedchild-keeps-cloneelement-in-1-0-1.md) | **Kevin's call** — *"oh, I didn't know we were still using `useClonedChild`. We probably shouldn't anymore, but i'll leave that for later."* 1.0.1 merges the **refs** (and the handlers) inside the clone and changes nothing else; whether a slot should be built on `cloneElement` at all is **open and deliberately not started**. Three defects have now come out of the same root — M6b's silently dropped `Field` attributes, M6c's `Menu`+`Tooltip` ref collision, and the caller's *own* ref being discarded on the element they wrote — and they are one defect wearing three hats: **`cloneElement` is a per-key replace, and a slot's props are not all values.** Deferred rather than done because the replacement (`Slot`/`asChild`, or `render` props) changes how a consumer writes the composition, which is a **2.0.0** with a migration and five apps already on `^1.0.0`; and because half-starting it — an `asChild` on two of four call sites — means two composition models in one library, plus a Preact mirror whose ref lives on the vnode and would need the whole thing built twice. What no amount of merging closes is the *type* hole (`Children.only` returns props typed `any`), which is the strongest argument for the rewrite and is not costing anyone data today. |
| 2026-08-02 | [`SortableTableHeader`, `Toast`/`ToastRegion` and `FileDropZone` ship in 1.0.0](2026-08-02-sortabletableheader-toast-and-filedropzone-ship-in-1-0-0.md) | **Kevin's call** — *"We talked about this already. They do have consumers, but they're in other spots."* — recorded with the factual state rather than a manufactured agreement, because the "three consumer-less components" framing was **already out of date by two of them**. Measured 2026-08-02 with `rg -uu` (a plain `rg` from the parent returns zero across every nested repo and reads exactly like "nobody does this"): `SortableTableHeader` has a real migrated consumer in mux-magic's `FileExplorerModal` (4 columns), and `Toast` has **two** — image-viewer's `FileBrowser` and `PaneGallery`, both taking `Toast` **without** `ToastRegion` and supplying their own `<ul>`, deliberately, because `ToastRecord` has no slot for an "Open N folders" control; that is the split being adopted at the seam it was designed to have. **`FileDropZone` alone is at zero**, and the fleet's two candidate drops are both *text*: gallery-downloader reads `getData("text")` and never `.files`, mux-magic's YAML path is paste-only. Eight real file-drop sites exist and all eight are in **bambuddy**, which the library does not reach. The sortable-header defect class is **three sites, not four** — bambuddy's `ForecastPanel` and `InventoryPage`, spoolbuddy's `SpoolsTable`, none with a `<button>` and none with `aria-sort` — uc-research's are PrimeNG, which emits `aria-sort` itself and is not an instance. Still worth shipping on: axe has **no rule** for a missing `aria-sort`, so withholding the component until a migration needs it would gate the fix on the thing the fix is for. |
| 2026-07-31 | [ePaper restricts colour by what is being drawn, not by what the panel can render](2026-07-31-epaper-restricts-colour-by-what-is-drawn-not-by-the-panel.md) | **Kevin's challenge, and the profile's own docstring was the bug.** *"the palette is a fixed six colours … that the panel can physically render"* is **false** — six is what one **pixel** can be, not what the **panel** can show, and the fleet's photo path has always depended on the difference (`immich_impression_frame.py` hands images straight to Floyd–Steinberg at `saturation=0.5`). Restated as a rule keyed to **what is being drawn**: photographs get the full dithered gamut, flat fills get **19** colours, and only borders/small text/icons keep the six — that last one right all along and now carrying its citation instead of an assertion. The recovered fidelity is `spectra6Blends`, **thirteen** two-ink 50% checkerboards, each reached by authoring **one flat hex** (the midpoint in the quantizer's 8-bit space) — so the Satori-side dither the research doc thought this needed was never needed; the consumer already did the work. **Measured, not predicted:** all fifteen ink pairs pushed through castkit's real `ditherToPanel` — 13 returned an exact 50.0/50.0 census, `black`+`yellow` came back **green+red** and `yellow`+`blue` came back **black+white** (chroma 26, exactly the neutral guard), and both are absent rather than present-and-wrong. Inks and blends are now **derived** from Pimoroni's two palettes, with the literals left only in the test, because the last time source and test both held the same invented set it passed for a whole milestone. Panels are keyed by **family** — `fixedInk` vs `continuousTone` — so `gallery3` (~50,000 colours, ACeP, **not in the fleet**) has somewhere to live that has no `inks` field to fill in wrongly; a variant axis was rejected for resurrecting the exact modelling error [the 2026-07-29 ADR](2026-07-29-epaper-is-a-profile-not-a-scheme.md) rejected. |
| 2026-07-31 | [`tokens` ships the first-paint snippet, and it is a `var()` fallback](2026-07-31-tokens-ships-the-first-paint-snippet.md) | **A live bug in three apps, and the reason the snippet stops being hand-copied.** An inline `<style>` is **unlayered**, and unlayered CSS beats every `@layer` — so `html, body { background-color: #131822 }` outranks `bg-surface-base` on `<body>` and **pins the canvas dark forever**: `data-scheme="light"` renders light chrome on a dark page and the header text vanishes into it. Nothing sees it — the CSS is valid, the utility is emitted and simply loses, and rip-deck's own test was *asserting the broken form*. Measured on the running dashboard: `rgb(19, 24, 34)` while `--color-surface-base` read `#F5F7FA`; `rgb(245, 247, 250)` after. Fixed in gallery-downloader, rip-deck and (latent, pre-M6b) mux-magic; castkit never had it. `tokens` now generates it — `buildFirstPaintRule(variant, scheme)` plus `dist/first-paint.css`, which is a **copy-me file, never `<link>`ed**, because a stylesheet request is the round-trip the rule exists to beat. Exporting the rule as a string is what makes a consumer's drift gate one `toContain`. |
| 2026-07-31 | [The RxJS kit is named `streams`, not `rx`](2026-07-31-the-rxjs-kit-is-named-streams-not-rx.md) | **Kevin's call, and the answer to "why is it called that."** `rx` is **ReactiveX** — the family convention RxJS inherits (`rxjs`, `@ngrx/*`, RxSwift, RxJava). Nobody chose it: it first appears in the plan at `d075eb0` as an agent's shorthand, ratified by no decision record. It **broke the scheme** — `tokens`, `logic`, `ui`, `eslint-config`, `biome-config` are what-it-is nouns; `rx` names the **dependency**, and a name that names the dependency lies the moment the dependency changes. `streams` covers both halves of the scope where `transport` covers only adapters, and matches what the code already calls these things (`useLogStream`, `openStateStream`, `/jobs/stream`). `tools` was rejected for importing `@mux-magic/tools`' vagueness. Nothing was published under either name, so there is no migration. Earlier docs saying `@charcuterie/rx` mean this package. |
| 2026-07-31 | [`streams` is a browser package, and push-only](2026-07-31-streams-is-a-browser-package-and-push-only.md) | **M7's scope, and it revises the plan's sketch.** `streams` owns server-pushed data arriving in a browser — SSE/WebSocket adapters plus backoff, grace windows, connection-state projection, cancellation. It owns **nothing else**, and two of the plan's four proposed contents are struck. The fleet's RxJS is **Node, not browser**: 266 files across 12 declarations, all but image-viewer's 20 renderer files server-side, and **zero** in mux-magic/web, castkit/slatecast, castkit/web, rip-deck/web. So the pipeline half is `jobRunner` + `sequenceRunner` + `taskScheduler` — 1 465 lines welded to `jobStore` and `AsyncLocalStorage`, one implementation, **not duplicated** — and MQTT has no browser caller at all (plex-channels' browser speaks SSE to its own server, which speaks MQTT onward). Request/response already has an owner: TanStack Query `^5.100.9`, already **tuned** in rip-deck. `taskScheduler` already documents the extraction cost — it injects `getActiveJobId` *"to keep `@mux-magic/tools` free of server imports"* — so doing it one repo further away pays again and buys nothing. |
| 2026-07-31 | [`streams` ships Observables and injected bindings, not hooks](2026-07-31-streams-ships-observables-and-injected-bindings.md) | **The M5b trap, avoided in advance.** M5b's finding was *"the component layer did not reach it"* — `ui` is React, slatecast is Preact. `streams`' worst-behaved candidate consumer is **that same package**: slatecast hand-rolls the fleet's most correct transport code (doubling backoff capped at 15 s, an `isStopped` flag, an onerror/onclose idempotence guard, and a `try`/`catch` around `new WebSocket` because the constructor throws synchronously offline — left unhandled *the reconnect loop dies for good*). Ship hooks and M7 designs that failure a second time knowing the outcome. So: framework-free core, `@charcuterie/streams/react` + `/signals` bindings, `tokens ← logic ← streams`, `ui` never imports it. The seam is proven — `logic` reaches slatecast via `createStoreFromSignals` today. `streams` **produces** `logic`'s `ConnectionStatus` rather than inventing a vocabulary, and carries the `reconnecting → reconnecting` guard castkit wrote by hand, because that is transport behaviour. First consumer: slatecast. |
| 2026-07-31 | [`streams` is built on RxJS, and the budget objection was measured](2026-07-31-streams-is-built-on-rxjs.md) | **`rxjs@^7.8.2`, the same range the other 12 declarations already use — exposed, not wrapped.** The one serious objection is bundle size in browsers that today have none, and it loses to its own numbers: measured with esbuild + `gzip -9`, a minimal surface is **5 952 B gz**, a realistic 20-symbol transport set **8 342 B**, and the **whole library un-shaken 18 465 B** — against slatecast's **40.3 KB of headroom** (19.7 KB of 60). The un-shaken figure is the one that matters, and it still leaves 21 KB. Guessing instead of measuring is how the ePaper palette got invented; transport is cheaper to measure than ink. Cancellation is the thing every hand-rolled client gets wrong — `isStopped`, `unmountedRef`, and `jobRunner`'s `subscription.add()` teardown *"without this… the sequenceRunner would await a child forever"* — and unsubscription is first-class in exactly one approach. A callback façade would pay the bundle cost and throw away composition. Only `tokens` stays zero-dep; `streams` **is** a runtime. |
| 2026-07-31 | [`packages/conformance` is not a package](2026-07-31-conformance-is-not-a-package.md) | **Dropped, with none of its assertions dropped.** The plan's private `@charcuterie/conformance` was to build React19+TWv4 / Preact / Satori profiles and assert slatecast's 60 KB gz budget — and checked against what the library became, **`@charcuterie/docs` *is* the React+Tailwind profile**, `packages/logic`'s five-adapter suite *is* the Preact one, the Satori profile has **nothing to render** (`Text` and `Stack` were never built, and M5b settled that the component layer does not reach a non-React consumer), and the 60 KB budget is castkit's, on a bundle this repo cannot build. "No animation survives an ePaper render" has no CSS to inspect either: the ePaper profile emits **zero** CSS by design, being resolved literals for a Satori consumer that cannot evaluate `var()`, and `epaper.test.ts` already pins six `0ms` durations. What genuinely had no gate was the claim all three profiles rest on — **which runtime an import forces you to install** — so `sourceRules.test.ts` gained it, beside the dependency-direction test the plan also asked for and which landed the same way: `tokens` reaches nothing, `logic/core` reaches nothing, **`logic/preact` never reaches `react`**, `ui/testing` reaches nothing, and every specifier reached must be a declared dependency. A second suite that renames the first is not coverage; it is a place to forget to update. |
| 2026-07-31 | [A menu is named by its trigger](2026-07-31-a-menu-is-named-by-its-trigger.md) | **M6, and it narrows the M3/M4 naming rule rather than repealing it.** `Menu` was written with a required `label`, by direct analogy with `Popover`'s `heading` — and `useRole` puts `aria-labelledby` on the panel pointing at the trigger, which **beats `aria-label`**. The menu was already named "Bay 3", from the button, and the prop was discarded with no type error, no lint, no axe violation, and an identical render. It is also the APG menu-button pattern. So the rule is: **an overlay with no trigger relationship needs its own name** — `Spinner`, `Modal`, `Popover` — and a menu has one, so the requirement moves to the trigger, where `Button`/`IconButton` already enforce it. Keeping the prop would have meant defeating `useRole` and letting a menu's name disagree with its own button. |
| 2026-07-31 | [An accordion panel is a `group`, not a landmark](2026-07-31-an-accordion-panel-is-a-group-not-a-landmark.md) | The APG says a panel *may* take `role="region"` and warns against "landmark region proliferation" in the same breath; the warning arrived on the first board, where four accordions over the same three sections produced four landmarks named "Disc" and failed axe's `landmark-unique` — which a real page listing jobs reproduces exactly. Dropping the role outright was the second attempt and was **wrong in a way that looked right**: `aria-labelledby` on a roleless `<div>` is inert, so the panel would have lost its name too (caught by `useAriaPropsSupportedByRole`, the one lint rule right about this file). `group` is the role the caveat is reaching for — "not intended to be included in a page summary" — and `<fieldset>`, which Biome suggests for it, is a *form-control* grouping that drags `<legend>` semantics onto prose. |
| 2026-07-31 | [`Select` is uncontrolled because the platform owns the value](2026-07-31-select-is-uncontrolled-because-the-platform-owns-the-value.md) | The first component in `@charcuterie/ui` that owns **no** state: a `<select>` holds its value in the DOM, and a `useSinglePicker` beside it is the Radix argument from `Popover` aimed at ourselves — the same conflict `Accordion` found live in mux-magic's `<details>`-plus-Jotai reconciliation. The reader's test is "does the platform already store this": a tab bar's selection has no DOM representation, a select's does. Native also brings type-ahead, Home/End/PageUp/PageDown, the mobile wheel picker, form submission, `:invalid` and autofill, and collapses **twelve** near-identical fleet selects. The **six** hand-rolled `role="listbox"` controls are not its callers — every one of them filters, so they are `Combobox` (P2); a bare `Listbox` none of them could adopt would have been a component with no caller. |
| 2026-07-31 | [Not every boolean is a state kind](2026-07-31-not-every-boolean-is-a-state-kind.md) | A component reaches for `@charcuterie/logic` when its state is **registered**, **composed**, or **shared** — and being a boolean is not a reason. `LogViewer`'s `isFollowing` is `useState`, because `useVisibility` would have fit mechanically at the cost of `isVisible` meaning "is following", which is how a state layer stops meaning anything. M6 closed the opposite question too: the plan expected `useLinkedIds` in `Field`, and a field's ids are static exactly as `Tabs` found — leaving **`createLinkedIds` with no consumer across twenty-five components**. That is left standing rather than fixed; an honestly-unused kind is better information than a contrived caller, and whether it earns its place is a question for the 1.0.0 cut. |
| 2026-07-31 | [A file input has no role](2026-07-31-a-file-input-has-no-role.md) | `FileDropZone` is the **only** component allowed to use `getByLabelText` instead of `expectAgentDrivable`, because `<input type="file">` has no ARIA role at all: HTML-AAM defines none, testing-library matches it to nothing, and **Playwright's own role engine calls it a button** — so `page.getByRole` finds it in a real agent run while the test that exists to prove agent-drivability cannot. Adding `role="button"` would pass the gate by destroying the file-picker semantics it protects, and dropping the real input would delete the component's whole reason to exist (there is no keyboard gesture for dragging; WCAG 2.5.7). `page.getByLabel(…).setInputFiles(…)` is what an agent actually writes, so the test still makes the agent's query. **The most accessible file control is the one the role model has no name for.** |
| 2026-07-31 | [1.0.0 is cut at the end of M6, not M7](2026-07-31-one-point-oh-cuts-at-the-end-of-m6.md) | **Kevin's call.** All five packages go to 1.0.0 **when the last consumer is migrated** — which is the end of **M6** (`xander` is last in its fleet list), not M7, because **M7 ships no code**: it is `@charcuterie/rx`, design doc + ADR only. The bump is its own `major` changeset after the last consumer branch merges, not bundled into it. The argument is not ceremony: pre-1.0 **the minor is the breaking channel**, and `^0.1.0` means `>=0.1.0 <0.2.0` — which bit on this very release. `tokens@0.2.0` replaced five of six ePaper values and says *"breaking for anyone reading these literals"*, yet every consumer pinned `^0.1.0`, so the corrected palette reaches **none** of them, **silently** — the range resolves happily to the old version instead of failing. After 1.0.0, `^1.0.0` takes every minor and breaking goes to 2.0.0, where an install stops and someone reads the changelog. Waiting for the last consumer is the point: 1.0.0 claims the API survived contact with every app, and M5b's *"the component layer does not reach a Preact consumer"* is exactly the finding that must not arrive after a stability promise. |
| 2026-07-31 | [ePaper is exempt from the contrast gate, and its palette comes from the consumer](2026-07-31-epaper-is-exempt-from-the-contrast-gate.md) | **M5b's first finding, and the argument for consumer milestones in one line.** The Spectra 6 palette here was invented — plausible primaries, and **not one of them a colour castkit's quantizer maps 1:1**, so none of the six survives a render. Measured through the real pipeline that is milder than the profile's own docstring claims — a flat off-palette field converges to one colour with no speckle — but it means the authored value is never the value on the panel. Worse, the paper was `#FFFFFF`, which an E6 panel cannot produce; the fleet renders at `saturation` 0.5, targeting `#D0D2D2`, and the ink the eye receives is `#A1A4A5`. Against real ink only **black on paper (8.37)** and **black on yellow (11.14)** reach AA — blue and green clear the 3:1 non-text bar, red clears nothing. Kevin's call: *"exempt ePaper from the gate, just fix the hexes"* — enforcing AA would reduce a six-ink panel to black, and WCAG's maths is for emissive displays, not a reflective panel read across a room. The numbers are recorded in `epaper.ts` rather than gated. Values copied with provenance (Pimoroni `inky_e673.py` via `castkit/packages/core/src/panels/palette.ts`), never imported — tokens keeps its zero dependencies, and the dependency would point the wrong way. |
| 2026-07-31 | [`!important` is for Storybook's chrome only, and it needs the exact `biome-ignore`](2026-07-31-important-is-for-storybook-chrome-only.md) | **Kevin's call, correcting the record below on one point.** `!important` is **never** used in `@charcuterie/ui` or `@charcuterie/tokens` — there we own every rule in play, so anything that seems to need priority is a wrong selector, layer or token. It **is** allowed in the docs stylesheet, which overrides Storybook's runtime-injected emotion, and the props table now uses it: one class plus priority, instead of the three class repetitions that were racing a Storybook bump. The earlier "`!important` is not available" was a suppressible lint rule misread as a missing feature — `lint/complexity/noImportantStyles`, recommended with an **unsafe** fix, so `yarn lint` strips it. The trap worth naming: a *wrong* rule id in the `biome-ignore` is stripped exactly like no comment at all, silently, under a comment claiming it is handled. Use `biome explain <ruleName>` for the category rather than guessing the group. |
| 2026-07-31 | [The whole docs page is themed by our tokens](2026-07-31-the-docs-page-is-themed-by-our-tokens.md) | **Kevin's call, superseding the M4 review's split.** Prose, tables, code and Prism's syntax colours all read `--color-*`, so a docs page follows scheme *and* variant like the canvas does. In **CSS**, not the theme object: a theme object cannot hold a `var()` (polished throws), so it would restate our palette as literal hex — **four variants x two schemes = eight** of them to keep in sync, where a `var()` is zero. Three things learned: `.sb-unstyled` is the wrong guard (Storybook puts it on heading anchors too), Storybook **doubles its own emotion class** on the props table so ours repeats three times, and **`biome --write --unsafe` silently deletes `!important`** while keeping the declaration. The gate is now WCAG contrast on six pairs per docs page — it caught a 1.14:1 failure the author had missed, and deleting one rule produces 64 red entries. |
| 2026-07-30 | [The shipped fonts are Baloo 2, Outfit and Victor Mono](2026-07-30-the-shipped-fonts-are-baloo-outfit-victor-mono.md) | **M5, and the first token change visible without any component change.** Charcuterie shipped *no* typeface — every variant spread `defaultTypography` untouched, so the system rendered in Segoe on the Windows boxes and Roboto on the Pis. `fontFamily` gains **`display`**, because a display cut at 24px is doing a different job from body copy at 13px. Fraunces had the better technical story (`SOFT`/`WONK` are real axes) and lost anyway: a display serif reads editorial next to a progress bar. Fonts ship from the **tokens** package, not `ui`, because a Satori consumer needs the woff2 and should not install React to get one — and `fonts.css` is a separate entry point so importing tokens never forces a font download. |
| 2026-07-30 | [The shipped mono is Victor Mono](2026-07-30-the-shipped-mono-is-victor-mono.md) | **A licensing decision, not a taste one.** Kevin asked for Dank Mono and holds a licence; its EULA permits use on *"any number of devices, websites"* — the whole fleet — but forbids copying and redistribution, and this repo is public with `access: public`. Buying the commercial licence does **not** help: the copying restriction is separate from the personal/commercial split. Victor Mono is the closest OFL analogue, being the only free candidate with ligatures **and** a genuinely cursive italic; apps override `--font-mono` with their own copy. Recorded because JetBrains Mono already had ligatures, so ligatures alone were never the reason to switch. |
| 2026-07-30 | [A consumer milestone adds components](2026-07-30-a-consumer-milestone-adds-components.md) | **M5.** A migration that finds a shape the app spells more than once builds it in `@charcuterie/ui` *in that milestone*, rather than leaving it in the app or deferring it to the next P-tier. rip-deck's largest duplication is a **banner**, which no P0 component is, so M5 could not meet its own stated proof (`TONE_CLASS` declared zero times) by spending the existing thirteen. The bar is evidence — duplicated within the consumer, or corroborated by the fleet inventory — because "the consumer needs it" would license moving `HeldBayCard` into the library too. Added `Alert` and `SegmentedControl`. |
| 2026-07-30 | [Tab selection is a `SinglePicker`](2026-07-30-tab-selection-is-a-single-picker.md) | **Kevin's call, and it supersedes the M4 verdict on one point.** `Tabs` composes `SinglePicker` + `RovingFocus`, not `VisibilityGroup` + `RovingFocus`: the thing that *registers* in a tab bar is the tab, so the registered set is the set of options and the panel's `hidden` is derived. Behaviour-identical — 53 ui-dom tests and 78 story mounts stayed green with **no test edited**, and that equality is the finding rather than a pass. The payoff is downstream: a tab bar with no panels at all had to model its choice as a group of visibilities it did not have, which is why `SegmentedControl` could not exist before this. |
| 2026-07-30 | [A tab bar is a scroll container](2026-07-30-a-tab-bar-is-a-scroll-container.md) | `Tabs` claimed in three places that a tab bar *scrolls, it does not wrap* and never implemented it — the tablist had no `overflow`, so a narrow bar painted its last tabs across the panel beside it, with a green test saying `scrollWidth > clientWidth` (equally true of a bar overflowing **visibly**). The scrollbar is hidden because a classic one takes layout space *inside* the container and would make the narrow bar taller than the wide one. And because `overflow` clips descendants' **outlines**, `--focus-ring-offset` is re-pointed inward on the tablist — re-pointing the token the shared `FOCUS_RING_CLASS` already reads, rather than appending a second `outline-offset` utility and letting Tailwind's ordering pick. |
| 2026-07-30 | [Docs `<Canvas>` blocks carry our surface](2026-07-30-docs-canvas-blocks-carry-our-surface.md) | Reported as "dark mode is missing on Docs"; the measurement said dark mode was fully on and `.sbdocs-wrapper` was `#FFFFFF`, so `--color-content-primary` (`#EDF0F5`) was painting at ~1.1:1 on every docs page. A docs page is Storybook's document *containing* our canvas — so the preview block takes our surface and the prose chrome keeps Storybook's theme. Not `parameters.docs.theme`: a parameter cannot read a global, so that fixes dark and breaks light. Feeding `var()` into Storybook's theme object throws in its `polished` colour maths. |
| 2026-07-30 | [Stories are demos; the DOM tests are `*.test.tsx`](2026-07-30-stories-are-demos-tests-are-tests.md) | **Supersedes the M3 testing record.** No story has a `play`; every DOM assertion is a `*.test.tsx` beside its component that mounts the **composed story** through `run()` — same chromium, same tokens, same axe, so there is still no second rendering stack. What it buys is that a test-shaped story can now be *deleted*: `Disabled Does Not Fire` was a test in the sidebar, `Tabs.AllStates` asserted three unrelated things including a stub proving `expectAgentDrivable` itself. Two things `storybookTest()` was quietly providing had to be rebuilt — the a11y addon only *throws* when `VITEST_STORYBOOK === "false"`, and `globalTypes.defaultValue` is canvas-only, so composed stories were rendering at `data-density="undefined"`. |
| 2026-07-30 | [A `Badge` is capped at its container](2026-07-30-badge-truncates-and-says-so.md) | `shrink-0` + `whitespace-nowrap` + no maximum let a long label paint across the next column. `max-inline-size: 100%` caps it; `overflow` decides what happens next — `truncate` (default) or `wrap`. Truncation is `text-overflow` and nothing else, so the ellipsis is *painted* and `textContent` survives: triple-click, copy, and screen readers all still get the whole string. The one remaining gap — a sighted pointer user — gets a `title`, applied only while the text is measurably clipped. `wrap` exists because `title` does not work on touch. |
| 2026-07-30 | [A prop typed from another package needs explicit `argTypes`](2026-07-30-cross-package-prop-types-need-explicit-argtypes.md) | `react-docgen` follows relative imports and stops at bare specifiers, so `size?: ControlSize` arrived as an opaque name and Storybook fell back to the **object control** — a `{}` textarea on a prop that takes three strings. Options are derived from the `Record<Union, …>` maps the components already index, so they cannot drift. `react-docgen-typescript` would resolve them and expand `ComponentPropsWithRef<"button">` into every HTML attribute React knows. Also: Storybook has not seeded `args` from docgen since v7, so every meta restates its own defaults. |
| 2026-07-30 | [The state layer is Charcuterie's model on `@floating-ui/react`](2026-07-30-state-layer-is-charcuterie-on-floating-ui.md) | **M4, and the plan's last open architecture question.** Not Radix / Base UI / Ark UI — all three own `open`, so wrapping any leaves `useVisibility` and the library both holding it. floating-ui is the layer all three are built on and is controlled by construction. **Tabs was the falsification test and the layer survived**: `automatic` vs `manual` activation is *one line*, because focus and selection are separate kinds. Three findings — `createLinkedIds` correctly does not fit Tabs; registration-based state was empty on its first paint in two kinds (a tab bar Tab could not enter); and `expectAgentDrivable` was wrong about roving groups. |
| 2026-07-30 | [Overlays reach the top layer through the platform, never a portal](2026-07-30-overlays-use-the-top-layer-not-a-portal.md) | **M4.** `Modal` is `<dialog>` + `showModal()`, `Popover` is `popover="manual"` — both stay where they are in the DOM. The deciding reason is not `z-index`: a portal moves the node to `document.body`, **out of the element a test or an agent scoped its queries to**. `manual` not `auto`, because the UA's light-dismiss closes the element by itself and leaves `isVisible` true. Two UA defaults must be undone and both look deliberate when wrong. |
| 2026-07-30 | [The modal scrim is its own token role](2026-07-30-scrim-is-its-own-token-role.md) | **M4.** `--color-scrim`, beside `focus` and `elevation` rather than inside `surface`: it is deliberately translucent (so it would break the opaque-hex rule) and nothing is ever drawn on it (so the contrast matrix would be gating a pair that cannot exist). Per variant, per scheme — `legible` dims at 0.84 and `daylight` at 0.66. ePaper's is `transparent`, which is a statement, not an omission. |
| 2026-07-30 | [Storybook reads the built `dist`, so the build runs first](2026-07-30-storybook-reads-the-built-dist.md) | **M4, found the expensive way.** `yarn storybook` and `yarn build:storybook` run `yarn build` first — do not remove it. A three-commit-old `dist` made a new token silently absent, a `logic` fix have no effect, and a story pass against Chromium's default `::backdrop`. Resolving the `source` condition instead was rejected: mirroring the consumer path is what M1 proved. Freshness is now a red test. |
| 2026-07-29 | [The preview eagerly imports `@storybook/addon-docs/blocks`](2026-07-29-preload-docs-blocks-before-the-focus-patch.md) | **M3 follow-up, and it looks like dead code — it is not.** Storybook 10.5.5's `enhanceContext` loader swaps `HTMLElement.prototype.focus` for an accessor whose getter reads `this.ownerDocument`; React Aria, inside Storybook's own lazily-loaded docs blocks, reads that property *on the prototype* at module scope and gets `Illegal invocation`. Cold-loading a docs page worked, clicking to one after any story did not — so all twelve docs pages were broken while every gate stayed green. The import fixes the ordering; `yarn smoke:storybook` is the gate that can see it. |
| 2026-07-29 | [Stories are the DOM test surface](2026-07-29-stories-are-the-dom-test-surface.md) | **M3.** No `*.test.tsx`, no jsdom, no second rendering stack in `@charcuterie/ui`: every DOM assertion is a story `play`, run in chromium with axe at `test: "error"`, and every one calls `expectAgentDrivable(canvas, { role, name })` — the same query an agent writes. The node project keeps what stories cannot see (Tailwind candidates, exhaustive switches, clamping, boundaries). mux-magic's unit tests mostly restate the class map; they would have caught neither bug M3 found. |
| 2026-07-29 | [`theme.css` bridges our structural tokens onto Tailwind's namespaces](2026-07-29-theme-css-bridges-tailwind-namespaces.md) | **M3.** `--text-*`, `--leading-*`, `--shadow-*`, `--ease-*`, and `--spacing` are published in `@theme`, so `text-sm` / `shadow-low` / `p-3` are **ours** and density-aware. Every entry deliberately redefines an existing utility — the same argument M1 accepted for `radius`/`tracking`, but stated in one listable place and pinned by `THEME_BRIDGES`. `variables.css` keeps our own names for the plain-CSS and Satori consumers. |
| 2026-07-29 | [The `cq-*` container-query variants are generated](2026-07-29-container-query-variants-are-generated.md) | **M3.** A container query's condition cannot be a `var()`, so `theme.css` emits `@custom-variant cq-sm (@container (min-inline-size: 24rem))` with the literal taken from the scale. Not Tailwind's `@sm:`, which reads the `--container-*` namespace we deliberately moved off. **And a container may not query itself** — `@container cq-md:px-8` on one element compiles, generates CSS, and never fires; now a test. |
| 2026-07-29 | [A `role="status"` region carries `aria-label`](2026-07-29-status-regions-carry-an-aria-label.md) | **M3.** A live region takes **no accessible name from its content**, so `Spinner` and `LiveStatusIndicator` set both: text for the announcement, `aria-label` for the name. Without it `getByRole("status", { name: "Loading…" })` matches nothing — announced but unfindable, and axe is silent. `Badge` gets no role at all, for the mirror-image reason. |
| 2026-07-29 | [Ship no icons — and no symbol glyphs in a default](2026-07-29-ship-no-icons-and-no-symbol-glyphs.md) | **M3.** The plan's "own no icon set" extended one step: a component's own default may not be a symbol character either, because `⚙`/`↶`/`▨`/`⚠` render as *nothing* wherever the font lacks them — this sandbox's chromium, and the minimal kiosk and ePaper images. `MediaTile`'s fallback is the words "Image unavailable"; story boards use excluded-from-build story SVGs. |
| 2026-07-29 | [Five state kinds, not three](2026-07-29-five-state-kinds.md) | **M2.** Adds **RovingFocus** (focus is not selection — arrowing a listbox must not fire the form's `onChange`) and **Status**, a ~120-line typed FSM, to the talk's original three. Deliberately not XState, which owns state — the same conflict that rules out Radix for overlays. All member-having kinds store *intent* and derive the public answer from intent + who is mounted, which makes the invariants true by representation. `VisibilityControlProvider` → `VisibilityGroup`, answering v1's own `NOTES.md`. |
| 2026-07-29 | [Store injection, not a hard Jotai dependency](2026-07-29-store-injection-not-a-jotai-dependency.md) | **M2, and the one decision that could not be retrofitted.** Three members — `get`, `set`, `subscribe` — with a 20-line observable-ref default; `logic/jotai` and `logic/signals` are ~30-line optional adapters. Jotai's scoping is already paid for by context, and costs ~5–6 KB `slatecast` won't pay. The same model-based suite runs against all three stores, so proving an adapter costs one line. |
| 2026-07-29 | [The logic hooks are uncontrolled](2026-07-29-logic-hooks-are-uncontrolled.md) | **M2.** `isVisible` / `visibleKey` / `selectedValue(s)` / `activeValue` are **initial** values, read once. No controlled mode, no prop-sync effect — that is the thesis (Charcuterie owns the state), and v1's sync effects are exactly the echo loop the fleet already has. `onChange` fires on intent changes only, never on registration churn. |
| 2026-07-29 | [The container-query scale is `--cq-*`](2026-07-29-container-query-scale-is-cq-not-container.md) | **Kevin's call.** Tailwind v4 owns `--container-*` for its `max-w-*` scale at *different* sizes, so ours silently turned `max-w-md` into `max-w-lg`. Renamed rather than adopting Tailwind's values: nothing about a *visual direction* argues for changing what `max-w-md` means. Radius/tracking/font collisions stay — those carry a variant's character. Free exactly once, before publish. |
| 2026-07-29 | [The `is`/`has` rule has no external-API carve-out](2026-07-29-is-has-rule-has-no-external-api-carve-out.md) | **M2.** The carve-out is that `property` is not selected, so an *object-literal* key like floating-ui's `open:` never fires; a `typeProperty` is a shape we own and stays flagged. Foreign booleans we must *write* (React's `IS_REACT_ACT_ENVIRONMENT`) route through `Record<string, boolean>` rather than a declared type property. No `eslint-disable` — `reportUnusedDisableDirectives` is on. |
| 2026-07-29 | [Consumers link tokens by `portal:` until it publishes](2026-07-29-consumers-link-tokens-by-portal-until-publish.md) | A consumer branch proving the integration depends on the package by relative path and **stays unmerged** until npm publish. Copying the built CSS would prove the stylesheet looks right without proving the package *resolves* — which is exactly what broke during M1. Publishing first would put a version on the registry before any workflow existed to produce it. |
| 2026-07-29 | [The `v2` root is a Yarn 4 workspace; v1 parks in `packages/logic`](2026-07-29-v2-root-is-a-workspace-and-v1-parks-in-packages-logic.md) | v1's Yarn 3 PnP / rollup / jest / Storybook 7 tooling is deleted from `v2`; its 30 source files move intact to `packages/logic/src` with **no `package.json`**, so Yarn ignores them and nothing can depend on unported code. Excluded from Biome, ESLint, Vitest, and typecheck in one place each; M2 removes those as it ports. `master` still holds working v1. |
| 2026-07-29 | [Logical properties only, enforced on `className`](2026-07-29-logical-properties-only-enforced-on-classname.md) | `ps-`/`pe-`, `ms-`/`me-`, `start-`/`end-`, `text-start`/`text-end` — never `left`/`right`. Enforced by `no-restricted-syntax` from `@charcuterie/eslint-config`, scoped to `className` literals **and template chunks** and nothing else: matching `left`/`right` as property names would fire on `getBoundingClientRect().left`, and a rule that cries wolf gets switched off. The pattern's anchors keep `border-red-500` / `rounded-lg` / `place-items-center` clean. |
| 2026-07-29 | [`@charcuterie/tokens` is a separate zero-dependency package](2026-07-29-tokens-is-a-separate-zero-dependency-package.md) | Not a subpath of `ui`: `castkit/packages/views` needs values for Satori with no React tree, and `slatecast` has 60 KB gz. Different peer dependencies is the signal; tree-shaking is not the argument. React consumers still see one name via `@charcuterie/ui/tokens` — build-graph split, not API split. |
| 2026-07-29 | [**`daylight` is the default visual direction**](2026-07-29-daylight-is-the-default-visual-direction.md) | **M0 winner.** Light-first, cool neutrals, blue-violet accent, roomier rows, restrained motion. The other three stay as working `data-variant` alternates. Default *scheme* stays `dark`, so M1's "looks identical after the token swap" proof still means something. |
| 2026-07-29 | [ePaper removes animation, not just shortens it](2026-07-29-epaper-removes-animation-not-just-shortens-it.md) | Loop durations are tokens; ePaper and `prefers-reduced-motion` set `animation: none` rather than a `0ms` duration, because a zero-duration animation still holds keyframe zero — which for the indeterminate sweep is an empty bar, i.e. the exact "wedged drive" misreading it exists to avoid. Every moving affordance owes an opacity-free static fallback. |
| 2026-07-29 | [ePaper is a profile, not a scheme](2026-07-29-epaper-is-a-profile-not-a-scheme.md) | ePaper ships as `@charcuterie/tokens/epaper`, outside the three composing axes, because it *removes* capabilities (no hover, opacity, shadow, transition; six colours) rather than restyling them — and `data-scheme="epaper"` would wrongly imply a `data-variant` still applies. |
| 2026-07-29 | [Gate contrast on WCAG 2.2 AA, report APCA](2026-07-29-contrast-gate-wcag-report-apca.md) | WCAG 2.2 fails CI; APCA Lc is printed next to every number but never gated, since WCAG 3 is still a draft. Only pairs WCAG actually requires are gated — scoping 1.4.11 to real control boundaries took the audit from 65 reported failures to 7 real ones. Also fails on alias drift. |
| 2026-07-29 | [`colour` in TypeScript, `--color-*` in CSS](2026-07-29-colour-in-typescript-color-in-css.md) | Deliberate split: TS matches the fleet's British spelling (`e6Colour`, `colourMode`), CSS matches Tailwind v4's `@theme` namespace, which is the only one that generates `bg-*`/`text-*`. Renaming the CSS side silently produces zero utilities. |

## Still to be recorded

These were settled in the plan but are not yet in effect in code, so they get their record
when the code lands rather than now:

- ~~The state-layer choice: Charcuterie's model on `@floating-ui/react`~~ — **recorded
  2026-07-30**, when M4 put it in effect. See the row above.
- Lockstep versioning for `tokens`/`logic`/`ui` — waits for the publish workflow. M1
  linked consumers by `portal:` instead of publishing, so there is nothing versioned to
  lock yet.
- Temporal, not date-fns, and that the library owns no date logic — M6
