// Lint fixture. Every line here is a deliberate violation — see
// `src/houseRules.test.ts`. Not compiled by the package tsconfig.

export const done = true

export const visible: boolean = false

export const check = (enabled: boolean) => enabled
